// Override any global docker.runOptions (e.g. -u host uid) so the container
// runs with the same user the image was built for (gepabds under /root/.local/bin).
profiles {

    docker {
        docker {
            enabled    = true
            runOptions = ''
        }
    }
}